using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//PURPOSE: TO REMOVE VLURRINESS FROM PIXEL FONT
//USAGE: PLACE THIS ON AN EMPTY GAME OBJECT CALLED FONT FIX AD PUT THIS SCRIPT ON IT
public class FontFix : MonoBehaviour
{
    public Font[] fonts;
    // Start is called before the first frame update
    void Start()
    {
       // for (int i = 0; i < fonts.Length; i++)
        //{
        //    fonts[i].material.mainTexture.filterMode = FilterMode.Point;
           

       // }
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
